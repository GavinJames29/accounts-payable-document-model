# accounts-payable-document-model

model for processing finance documents used in accounts payable workflows

## Publishing changes

Create a branch for each update, commit and push the change, then open a pull
request on GitHub. Merge the pull request into `main` after its checks pass.
